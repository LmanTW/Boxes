#+data <- Empty

#+@function <- { Input(0) + Input(1) | Result > 0 | data = Result }

#+@main <- function(0, 1) | data # { type: 'boolean', value: 'No' }

+count <- 0
+@loop <- { count < 10 ? { count = count + 1 | print(count) | loop() } : { print('Stoped', count) } }

+@main <- print('Running') | loop()
